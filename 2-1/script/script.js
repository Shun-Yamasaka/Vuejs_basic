const app = new Vue({
    el:'#app',
    data(){                     // data関数を定義
        return {                // オブジェクトをreturnする
            text:'hello Vue.js' // textプロパティに文字列を設定
        }
    }
});